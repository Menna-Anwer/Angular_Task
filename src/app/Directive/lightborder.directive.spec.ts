import { LightborderDirective } from './lightborder.directive';

describe('LightborderDirective', () => {
  it('should create an instance', () => {
    const directive = new LightborderDirective();
    expect(directive).toBeTruthy();
  });
});
